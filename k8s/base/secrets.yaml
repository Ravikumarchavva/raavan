# ⚠️  SECURITY WARNING: This file contains PLACEHOLDER secrets for local Kind development ONLY.
# DO NOT commit real credentials here. For production deployments, use one of:
#   - Bitnami Sealed Secrets (kubeseal)
#   - External Secrets Operator + AWS Secrets Manager / HashiCorp Vault
#   - SOPS-encrypted secrets
# The values below are intentionally invalid and must be replaced.
apiVersion: v1
kind: Secret
metadata:
  name: shared-secrets
  namespace: af-edge
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@postgres.af-data.svc.cluster.local:5432/agentdb"
  REDIS_URL: "redis://redis.af-data.svc.cluster.local:6379/0"
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"
  OPENAI_API_KEY: "sk-CHANGE_ME"
---
# Secrets for services in af-platform namespace
apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: af-platform
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@postgres.af-data.svc.cluster.local:5432/agentdb"
  REDIS_URL: "redis://redis.af-data.svc.cluster.local:6379/0"
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"
  OPENAI_API_KEY: "sk-CHANGE_ME"
---
# Secrets for services in af-runtime namespace
apiVersion: v1
kind: Secret
metadata:
  name: runtime-secrets
  namespace: af-runtime
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@postgres.af-data.svc.cluster.local:5432/agentdb"
  REDIS_URL: "redis://redis.af-data.svc.cluster.local:6379/0"
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"
  OPENAI_API_KEY: "sk-CHANGE_ME"
