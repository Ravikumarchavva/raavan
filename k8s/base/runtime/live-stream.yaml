apiVersion: apps/v1
kind: Deployment
metadata:
  name: live-stream
  namespace: af-runtime
  labels:
    app: live-stream
    af.service-role: live-stream
spec:
  replicas: 2
  selector:
    matchLabels:
      app: live-stream
  template:
    metadata:
      labels:
        app: live-stream
        af.service-role: live-stream
    spec:
      serviceAccountName: live-stream
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: live-stream
          image: ghcr.io/example/agent-framework/live-stream:0.1.0
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 8017
              name: http
          envFrom:
            - secretRef:
                name: runtime-secrets
          env:
            - name: SERVICE_NAME
              value: live-stream
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: live-stream
  namespace: af-runtime
  labels:
    app: live-stream
spec:
  selector:
    app: live-stream
  ports:
    - port: 8017
      targetPort: http
      name: http
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: live-stream
  namespace: af-runtime
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: live-stream
  namespace: af-runtime
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: live-stream
  minReplicas: 2
  maxReplicas: 15
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
