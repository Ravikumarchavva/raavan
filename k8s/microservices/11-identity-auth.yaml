apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-auth
  namespace: af-platform
  labels:
    app: identity-auth
    af.service-role: identity-auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: identity-auth
  template:
    metadata:
      labels:
        app: identity-auth
        af.service-role: identity-auth
    spec:
      serviceAccountName: identity-auth
      containers:
        - name: identity-auth
          image: ghcr.io/example/agent-framework/identity-auth:0.1.0
          ports:
            - containerPort: 8010
              name: http
          envFrom:
            - secretRef:
                name: platform-secrets
          env:
            - name: SERVICE_NAME
              value: identity
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: identity-auth
  namespace: af-platform
  labels:
    app: identity-auth
spec:
  selector:
    app: identity-auth
  ports:
    - port: 8010
      targetPort: http
      name: http
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: identity-auth
  namespace: af-platform
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: identity-auth
  namespace: af-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: identity-auth
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
