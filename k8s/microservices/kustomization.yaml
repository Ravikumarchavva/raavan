apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Namespaces
  - 00-namespaces.yaml
  # Secrets (template — replace values for production)
  - 01-secrets.yaml
  # Wave 1: Edge + Platform auth
  - 10-gateway-bff.yaml
  - 11-identity-auth.yaml
  - 12-policy-authorization.yaml
  # Wave 2: Conversation + Job Controller
  - 20-conversation.yaml
  - 21-job-controller.yaml
  # Wave 3: Agent Runtime + Tool Executor + Code Interpreter
  - 30-agent-runtime.yaml
  - 31-tool-executor.yaml
  - 32-code-interpreter.yaml
  # Wave 4: Human Gate + Live Stream + File Store
  - 40-human-gate.yaml
  - 41-live-stream.yaml
  - 42-file-store.yaml
  # Wave 5: Admin
  - 50-admin.yaml
  # Network policies
  - 90-network-policies.yaml
